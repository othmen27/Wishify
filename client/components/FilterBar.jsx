// FilterBar.jsx
// “All / Active / Completed” toggles

import React from 'react';

const FilterBar = ({ filter, setFilter }) => {
  return (
    <div>
      {/* TODO: Implement FilterBar */}
      FilterBar Component
    </div>
  );
};

export default FilterBar; 